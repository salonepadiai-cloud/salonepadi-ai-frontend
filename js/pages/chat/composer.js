/*
|--------------------------------------------------------------------------
| COMPOSER FEATURE
|--------------------------------------------------------------------------
|
| Future location for slash commands, draft persistence, attachments,
| voice controls and richer composer behaviour.
|--------------------------------------------------------------------------
*/

export function init(context) {
  return () => {};
}
