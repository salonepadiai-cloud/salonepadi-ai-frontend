// JOHNNY TEC OS — global config
// Single source of truth for backend location. Nothing else in the
// codebase should hardcode this URL.

const CONFIG = {
  API_BASE_URL: 'https://salonepadi-ai-backend.onrender.com',
  APP_NAME: 'JOHNNY TEC OS',
};
